{
  "name": "agent-loadout",
  "version": "0.1.0",
  "description": "One command to load out your terminal for agentic coding",
  "type": "module",
  "bin": {
    "agent-loadout": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "prepack": "pnpm build && pnpm typecheck",
    "version": "pnpm build && git add -A dist"
  },
  "keywords": [
    "cli",
    "ai",
    "agent",
    "developer-tools",
    "loadout"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "chalk": "^5.4.0",
    "commander": "^13.0.0",
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
